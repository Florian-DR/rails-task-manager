<h1>Index</h1>
<p><%= link_to "Add a new task", new_task_path %></p>
<ol>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task)%> <%= link_to "🗑", task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></li>
  <% end %>
</ol>
